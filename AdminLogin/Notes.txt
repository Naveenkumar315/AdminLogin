1. To Use a form install npm - npm i react-hook-form

2. Define the Interface Outside the Component, inside interface value should not come coma, it should come semicolan

3. to exit a git = :qa and enter

4. Routing - need to import routes in app.tsx file

5. component name should always caps 
for example - <LeftMenu />

6. in map with html if we want to access debugger mode we need to use return method
7. each loop have unique key - this is for first child element

8. useState - always try update callback method. then only we update multiple time with same state.
with help of useEffect we can see the updated state value. 